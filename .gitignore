# Secrets — never commit these
.env
token.json
credentials.json

# Local state — cloud uses Gist
state.json

# Python
__pycache__/
*.pyc

# Logs
logs/

# Virtual environment
.venv/

# Claude Code local settings
.claude/settings.local.json
token.json.bak

# Private family details (CI uses the FAMILY_CONFIG secret)
family_config.json
